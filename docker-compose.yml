version: "3"
services:
  mysql8:
    image: mysql:8.4
    container_name: mysql_app
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: golang_user
    # volumes:
    #   - ./mysql:/var/lib/mysql:delegated
